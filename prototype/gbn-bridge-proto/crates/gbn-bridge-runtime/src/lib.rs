//! Placeholder home for shared Conduit creator and bridge runtime logic.

/// Phase 1 placeholder proving the runtime crate boundary compiles.
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct RuntimePlaceholder {
    /// Reserved for the future authority-plane endpoint configuration.
    pub publisher_url: Option<String>,
}
