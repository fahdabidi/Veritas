apiVersion: v1
kind: Service
metadata:
  name: publisher-authority
  labels:
    app.kubernetes.io/name: publisher-authority
    app.kubernetes.io/part-of: veritas-conduit
    veritas-role: authority
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: publisher-authority
  ports:
    - name: http
      port: 8080
      targetPort: http
