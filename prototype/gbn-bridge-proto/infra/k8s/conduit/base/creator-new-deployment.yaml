apiVersion: apps/v1
kind: Deployment
metadata:
  name: creator-new
  labels:
    app.kubernetes.io/name: creator-new
    app.kubernetes.io/part-of: veritas-conduit
    veritas-role: creator
    conduit-actor: new-creator
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: creator-new
  template:
    metadata:
      labels:
        app.kubernetes.io/name: creator-new
        app.kubernetes.io/part-of: veritas-conduit
        veritas-role: creator
        conduit-actor: new-creator
    spec:
      securityContext:
        fsGroup: 10001
      containers:
        - name: creator-runner
          image: veritas/creator-runner:dev
          imagePullPolicy: Never
          ports:
            - name: admin
              containerPort: 9090
          envFrom:
            - configMapRef:
                name: conduit-config
            - secretRef:
                name: conduit-signing
          env:
            - name: GBN_BRIDGE_NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GBN_CONDUIT_ACTOR
              value: new-creator
            - name: GBN_BRIDGE_INGRESS_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: GBN_BRIDGE_STATE_DIR
              value: /var/lib/gbn-conduit
          readinessProbe:
            exec:
              command:
                - sh
                - -ec
                - curl -fsS http://127.0.0.1:9090/v1/admin/node-metadata >/dev/null
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 18
          livenessProbe:
            exec:
              command:
                - sh
                - -ec
                - curl -fsS http://127.0.0.1:9090/v1/admin/node-metadata >/dev/null
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 75m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          volumeMounts:
            - name: state
              mountPath: /var/lib/gbn-conduit
      volumes:
        - name: state
          persistentVolumeClaim:
            claimName: creator-new-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: creator-new-state
  labels:
    app.kubernetes.io/name: creator-new
    app.kubernetes.io/part-of: veritas-conduit
    veritas-role: creator
    conduit-actor: new-creator
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: creator-new
  labels:
    app.kubernetes.io/name: creator-new
    app.kubernetes.io/part-of: veritas-conduit
    veritas-role: creator
    conduit-actor: new-creator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: creator-new
  ports:
    - name: admin
      port: 9090
      targetPort: admin
